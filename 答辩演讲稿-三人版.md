# 答辩演讲稿（三人各 1 分钟）

---

## 👤 项目经理（1 分钟）

老师好，我是项目经理，负责需求分析、进度管理和技术选型。

**项目背景**：我们发现一个真实痛点——所有 AI 助手都会失忆，每次开新对话就忘了上次聊的内容。用户每次都要重新介绍自己，非常低效。

**我们的方案**：做一个云端记忆库 REST API，叫 Convergent Memory。任何 AI（豆包、ChatGPT、Claude）都能通过 HTTP 接口读写用户的记忆，实现跨 AI 共享。

**技术选型**：Java 17 + Spring Boot 3.2.5 + Spring Security + JPA + AgentScope Java v2。选 Spring Boot 因为生态成熟、安全框架完善；选 AgentScope 因为它是阿里通义开源的 AI Agent SDK，专门做 LLM 编排。

**项目管理**：我们用 GitHub 协作，分三个迭代——第一周搭骨架和写入功能，第二周做搜索和收敛，第三周做多用户系统和部署。最终部署在 DigitalOcean 云服务器，通过 nginx 反向代理实现国内可达。

谢谢，下面请编码同学介绍技术实现。

---

## 👤 编码（1 分钟）

老师好，我负责核心编码。项目一共 31 个 Java 源文件，分为 6 层：entity、repository、service、controller、config、agent。

**核心设计三点**：

第一，**Markdown 是真身，数据库是索引**。每条记忆写入时同时生成一个 .md 文件落盘 + 一条 H2 数据库记录。文件用于长期保存，数据库用于快速搜索。

第二，**多用户隔离**。用 Spring Security 自定义 Filter 拦截 Bearer Token，查数据库匹配用户。每条记忆绑 ownerId，搜索时自动过滤——你只能看到自己的记忆。密码用 BCrypt 10 轮哈希存储。

第三，**两阶段写**。调用 `/converge/preview` 时 Agent 生成草案存 DB 但不写文件；用户确认后调 `/apply` 才真正落盘。这样 AI 不能偷偷修改用户的记忆，所有变更可审计。

部署方面，Spring Boot 跑在 8081 端口，nginx 做反向代理把 :80 请求转到后端，用 systemd 做进程管理。

谢谢，下面请测试同学。

---

## 👤 测试（1 分钟）

老师好，我负责测试。我从三个维度验证了系统的正确性。

**第一，接口功能测试**。一共 9 个 REST 接口，全部用 curl + Swagger UI 逐一验证。写入接口返回 200 + 记忆 ID；搜索接口支持中文关键词，按相关度打分排序；注册接口自动生成 Token 并返回完整链接。

**第二，安全测试**。不带 Token 访问写接口 → 返回 403 拒绝；用 A 用户的 Token 去读 B 用户的记忆 → 返回 401 拒绝；去掉 URL 里的 token 参数 → 页面只显示统计信息，看不到具体内容。多租户隔离验证通过。

**第三，可用性测试**。我们用手机 4G 流量（无 VPN）从国内直接访问美国服务器，响应时间 0.5 秒以内。并且实际让豆包 AI 通过 HTTP 调用我们的 API，成功完成了注册账号、写入记忆、读取记忆的完整流程——证明这个系统确实能被 AI 直接使用。

测试结论：9 个接口全部通过，安全隔离有效，跨境访问稳定。谢谢老师。

---

## 💡 使用提示

- 三人按顺序讲，每人控制在 60 秒
- 如果老师追问技术细节 → 编码同学接
- 如果老师问"为什么做这个" → 项目经理接
- 如果老师问"怎么验证的" → 测试同学接
- 如果老师问"能不能现场演示" → 打开浏览器访问 http://134.209.66.112/
